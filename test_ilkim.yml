---
- name: Find KB Docs
  hosts: all
  #become: true
  tasks:
    - name: Check File Path for KB Name
        find:
          paths: /data/ftp/files/
          patterns: '^\S{kb5011644}$'
          use_regex: yes
